"""Deprecated module. Kept temporarily to avoid import errors if referenced.

API routes have been moved to `app/api/games.py` and registered under `/api/games`.
This file can be safely removed once all imports are updated.
"""